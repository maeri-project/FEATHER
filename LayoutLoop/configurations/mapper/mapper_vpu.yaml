mapper:
  optimization-metrics: [ delay, energy ]
  live-status: False
  num-threads: 32
  timeout: 3000
  victory-condition: 3000
  algorithm: random-pruned
  max-permutations-per-if-visit: 8
  sync_interval: 100
  penalize_consecutive_bypass_fails: True